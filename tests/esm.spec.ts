// tests to check functionality in an ESM context - use a subprocess with the proper args?

describe("Should test using an ESM context", () => {
  test("Should run uploadFolder successfully", async () => {
    expect(true).toBe(true);
  });
});
